# Architecture Documentation

Files in this directory are historical design records unless explicitly marked current.

Current control semantics: `docs/governance/CURRENT_RUNTIME_RULES.md`.

If an architecture document conflicts with the current runtime rules, the current runtime rules win.
