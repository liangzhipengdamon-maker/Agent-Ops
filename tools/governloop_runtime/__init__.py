"""GovernLoop runtime — governed autonomy for coding agents.

This is the canonical public runtime namespace for GovernLoop v0.1.0.
The pre-release ``agentops_runtime`` package remains an internal compatibility
layer while the project completes its naming migration; new integrations
should import and invoke ``governloop_runtime``.
"""

__all__ = ["__version__"]
__version__ = "0.1.0"
